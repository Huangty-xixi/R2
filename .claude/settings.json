{
  "hooks": {
    "PreToolUse": [{
      "matcher": "Edit|Write",
      "hooks": [{
        "type": "command",
        "command": "python -c \"import sys,json; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); sys.exit(1 if p.endswith('.c') or p.endswith('.h') else 0)\" && echo '{}' || echo '{\"continue\":false,\"stopReason\":\"GBK编码文件! 请使用 /gbk-batch-edit skill 编辑 .c/.h 文件\"}'"
      }]
    }]
  },
  "permissions": {
    "allow": [
      "Bash(restricted)",
      "Read(*)",
      "Edit(*)",
      "Write(*)",
      "Glob(*)",
      "Grep(*)",
      "TaskCreate",
      "TaskUpdate",
      "TaskList",
      "TaskGet",
      "Agent(*)",
      "Skill(*)"
    ],
    "defaultMode": "acceptEdits"
  }
}
